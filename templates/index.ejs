<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <title>To-do v2</title>

    <style>
        body {
            background-color: darkgrey;
        }

        .concluido {
            text-decoration: line-through;
        }
    </style>

</head>

<body>

    <div class="container d-flex flex-column mt-5">
        <h1 style="text-align: center;">Tarefas</h1>
        <form class="d-flex justify-content-center" action="/" method="post">
            <input type="text" name="task" class="me-1 border border-black rounded">
            <button class="btn btn-dark">Add</button>
        </form>
        <div class="d-flex flex-column border border-black rounded p-2 mt-3">
            <% for( let index=0; index < docs.length; index++ ) { %>
                <form method="post" action="/?_method=DELETE" class="d-flex justify-content-between mb-2">
                    <div class="row">
                        <div class="col mb-2">
                            <div class="d-flex align-items-start" onclick="concluded(this)">
                                <input type="checkbox" class="check me-2 mt-1" style="min-width: 25px; min-height: 25px;">
                                <label class="label">
                                    <%= docs[index].task %>
                                </label>
                            </div>
                        </div>

                        <div class="row">
                            <div>
                                <input name="id" value="<%= docs[index]._id %>" hidden>
                                <a href="/edit/<%= docs[index]._id %>" class="btn btn-light">Edit</a>
                                <button type="submit" class="btn btn-secondary">Delete</button>
                            </div>
                        </div>

                    </div>

                </form>

                <% } %>
        </div>
    </div>


    <script>

        function concluded(div) {
            const input = div.children[0]
            const label = div.children[1]

            if (input.checked === true) {
                label.classList.add('concluido')
            } else if (input.checked === false) {
                label.classList.remove('concluido')
            }
        }

    </script>

</body>

</html>